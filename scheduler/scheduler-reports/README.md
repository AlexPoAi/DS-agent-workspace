# Legacy Scheduler Reports

`scheduler/scheduler-reports/` — это legacy historical path.

Сейчас свежие scheduler reports пишутся в:
- `scheduler/reports/`

Эту папку не считать live source-of-truth для текущего состояния среды.
Она сохранена как исторический хвост, пока не будет отдельного решения по архивной консолидации.
